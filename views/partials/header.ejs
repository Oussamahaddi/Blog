<header>
    <div class="logo">
        <img src="/images/logo.png" alt="">
    </div>
    <div class="nav_bar">
        <ul>
            <% for (let i of menu) { %>
                <% if (i.url == url) { %>
                    <a href="<%= i.url %>"><li class="active"><%= i.name %></li></a>
                <% } else { %>
                    <a href="<%= i.url %>"><li><%= i.name %></li></a>
                <% } %>
            <% } %>
        </ul>
    </div>
    <div class="avatar">

    </div>
</header>